# Kebs API base URL, including the /api prefix.
# Point this at the tunnel URL to demo from a phone.
VITE_API_URL=http://localhost:3001/api

# Optional one-click chips on /verify. Codes come from
# `npm run db:seed` + POST /batches/:id/codes in apps/api.
VITE_DEMO_CODES=
